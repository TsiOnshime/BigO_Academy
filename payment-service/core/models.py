"""
core/models.py — Payment Service

PLACEHOLDER. Django ORM models are an adapters-layer concern and are
intentionally not built in this pass — we're stopping at domain/ +
application/ this round.

When this gets built, expect one Django model per domain dataclass in
domain/models/ (SubscriptionPlan, Subscription, Invoice, Transaction,
TeacherPayoutRate, TeacherPayout), following the same pattern as
academic-service/core/models/ (a models/ package, one file per entity,
choices mirrored from domain/enums.py).
"""
